# Gaussian-LIC Setup

A streamlined setup for Gaussian-LIC with Livox lidar and MAVROS integration.

## Quick Start

```bash
# Clone the repository (with your fork URLs)
git clone https://github.com/your-username/gaussian-lic-setup.git
cd gaussian-lic-setup

# Run the setup script
./setup.sh